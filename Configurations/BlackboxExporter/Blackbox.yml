modules:
  http_2xx:
    prober: http
    http:
      preferred_ip_protocol: "ip4"
  http_post_2xx:
    prober: http
    http:
      method: GET
      basic_auth:
        username: '{{ .labels.username }}'
        password: '{{ .labels.password }}'
