apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: alertmanager-data
  name: alertmanager-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
