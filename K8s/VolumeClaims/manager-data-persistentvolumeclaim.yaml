apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: manager-data
  name: manager-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
